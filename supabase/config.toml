
# A string used to distinguish different Supabase projects on the same host. Defaults to the
# working directory name when running `supabase init`.
project_id = "zllrplxlewqyipxxpetl"

[api]
enabled = true
# Port to use for the API URL.
port = 54321
# Schemas to expose in your API. Tables, views and stored procedures in this schema will get API
# endpoints. public and storage are always included.
schemas = ["public", "storage", "graphql_public"]
# Extra schemas to add to the search_path of every request. public is always included.
extra_search_path = ["public", "extensions"]
# Maximum rows returns in a single REST request.
max_rows = 1000

[db]
# Port to use for the local database URL.
port = 54322
# Port used by db diff command to initialize the shadow database.
shadow_port = 54320
# The database major version to use. This has to be the same as your remote database's. Run `SHOW
# server_version;` on the remote database to check.
major_version = 15

[db.pooler]
enabled = false
# Port to use for the local connection pooler.
port = 54329
# Maximum number of connections to store in the pool for each user/database pair.
pool_size = 20
# Default pool mode to use. Can be "transaction" or "session".
pool_mode = "transaction"
# Default set of pool options to use. Can be "transaction" or "session".
default_pool_size = 20
# Maximum time to wait if the pool is full. In seconds.
max_client_conn = 100
# Maximum idle time for connections. In seconds.
default_client_idle_timeout = 600
# Time to check for idle connections. In milliseconds.
max_client_idle_timeout = 3600
# If the pool is so busy you can't get a connection from it after pool_timeout, you'll get a database
# connection without a pooler.
pool_timeout = 10

[studio]
enabled = true
# Port to use for Supabase Studio.
port = 54323
# External URL of the API server that frontend connects to.
api_url = "http://localhost"

# Email testing server. Emails sent with the local dev setup are not actually sent - rather, they
# are monitored, and you can view the emails that would have been sent from the web interface.
[inbucket]
enabled = true
# Port to use for the email testing server web interface.
port = 54324
# Uncomment to expose additional ports for testing user applications that send emails.
# smtp_port = 54325
# pop3_port = 54326

[storage]
enabled = true
# The maximum file size allowed (e.g. "5MB", "500KB").
file_size_limit = "50MiB"

[auth]
enabled = true
# The base URL of your website. Used as an allow-list for redirects and for constructing URLs used
# in emails.
site_url = "http://localhost:3000"
# A list of *exact* URLs that auth providers are permitted to redirect to post authentication.
additional_redirect_urls = ["https://localhost:3000"]
# How long tokens are valid for, in seconds. Defaults to 3600 (1 hour), maximum 604,800 (1 week).
jwt_expiry = 3600
# If disabled, the refresh token will never expire.
enable_refresh_token_rotation = true
# Allows refresh tokens to be reused after expiry, up to the specified interval in seconds.
# Requires enable_refresh_token_rotation = true.
refresh_token_reuse_interval = 10
# Allow/disallow new user signups to your project.
enable_signup = true

[auth.email]
# Allow/disallow new user signups via email to your project.
enable_signup = true
# If enabled, a user will be required to confirm any email change on both the old, and new email
# addresses. If disabled, only the new email is required to confirm.
double_confirm_changes = true
# If enabled, users need to confirm their email address before signing in.
enable_confirmations = false

[auth.sms]
# Allow/disallow new user signups via SMS to your project.
enable_signup = true
# If enabled, users need to confirm their phone number before signing in.
enable_confirmations = false

[auth.external.apple]
enabled = false
# Client ID for the Apple provider.
client_id = ""
# Secret for the Apple provider.
secret = ""

[auth.external.azure]
enabled = false
# Client ID for the Azure provider.
client_id = ""
# Secret for the Azure provider.
secret = ""

[auth.external.bitbucket]
enabled = false
# Client ID for the Bitbucket provider.
client_id = ""
# Secret for the Bitbucket provider.
secret = ""

[auth.external.discord]
enabled = false
# Client ID for the Discord provider.
client_id = ""
# Secret for the Discord provider.
secret = ""

[auth.external.facebook]
enabled = false
# Client ID for the Facebook provider.
client_id = ""
# Secret for the Facebook provider.
secret = ""

[auth.external.github]
enabled = false
# Client ID for the GitHub provider.
client_id = ""
# Secret for the GitHub provider.
secret = ""

[auth.external.gitlab]
enabled = false
# Client ID for the GitLab provider.
client_id = ""
# Secret for the GitLab provider.
secret = ""

[auth.external.google]
enabled = false
# Client ID for the Google provider.
client_id = ""
# Secret for the Google provider.
secret = ""

[auth.external.keycloak]
enabled = false
# Client ID for the Keycloak provider.
client_id = ""
# Secret for the Keycloak provider.
secret = ""
# URL for the Keycloak provider.
url = ""

[auth.external.linkedin]
enabled = false
# Client ID for the LinkedIn provider.
client_id = ""
# Secret for the LinkedIn provider.
secret = ""

[auth.external.notion]
enabled = false
# Client ID for the Notion provider.
client_id = ""
# Secret for the Notion provider.
secret = ""

[auth.external.twitch]
enabled = false
# Client ID for the Twitch provider.
client_id = ""
# Secret for the Twitch provider.
secret = ""

[auth.external.twitter]
enabled = false
# Client ID for the Twitter provider.
client_id = ""
# Secret for the Twitter provider.
secret = ""

[auth.external.slack]
enabled = false
# Client ID for the Slack provider.
client_id = ""
# Secret for the Slack provider.
secret = ""

[auth.external.spotify]
enabled = false
# Client ID for the Spotify provider.
client_id = ""
# Secret for the Spotify provider.
secret = ""

[auth.external.workos]
enabled = false
# Client ID for the WorkOS provider.
client_id = ""
# Secret for the WorkOS provider.
secret = ""

[auth.external.zoom]
enabled = false
# Client ID for the Zoom provider.
client_id = ""
# Secret for the Zoom provider.
secret = ""

[analytics]
enabled = false
# Backend URL for the analytics.
backend_url = "http://localhost:54327"
# Token for analytics service. This is used to validate requests.
token = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InpsbHJwbHhsZXdxeWlweHhwZXRsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDUwMDU3NzksImV4cCI6MjA2MDU4MTc3OX0.1TRnufxXW7jdyGLw260j3i_Wl4ciKhb_eMXc3G1uRtk"
# Vector dimension used for storing embeddings. Required.
vector_dimension = 384

[experimental]
# Configure OpenAI integration for the Dashboard.
openai_enabled = false
openai_api_key = ""

# Configure functions for various features.
[functions.gumroad-webhook]
verify_jwt = false

[functions.gumroad-checkout]
verify_jwt = true

[functions.verify-subscription]
verify_jwt = true

[realtime]
enabled = true
